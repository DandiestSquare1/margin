extends layout

block content
    div(ng-controller='StockCntrl' ng-init='ticker = #{JSON.stringify(ticker)}; build(ticker);')
        h1 {{stock.Name}} ({{stock.Symbol}})
        h2 {{stock.LastPrice | currency : symbol}}